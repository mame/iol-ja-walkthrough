+++
index = 2
lang = 'フリウリ語'
level = 1
tags = ['rosetta']
+++

# details

## ヒント 1

文の比較によって `nestri` や `mê` が所有者を表しているとわかるので、語順はおおよそ「定冠詞 - 所有者 - 所有対象」とわかる。

## ヒント 2

4 種類の定冠詞 `la` 、`il` 、`lis` 、`i` で文をグループ分けする。

### ヒント 2-1

定冠詞が無い文については、同じ所有者の表現を使っているグループに含める。
たとえば、`mê sûr`「私の姉（単数）」は `la mê cjase`「私の家（単数）」のグループに入れる。

するときれいにグループが分かれる。

### 答え

* 所有対象が男性名詞か女性名詞か
* 所有対象が単数か複数か

この 4 グループによって定冠詞が使い分けられる。

### ヒント 2-2

`mê` と `mês` の使い分け、`to` と `tôs` の使い分けも読み取れるはず。

### 答え

対象が単数なら `mê`/`to` 、複数なら `mês`/`tôs` のように、単複で使い分けられる。

## ヒント 3

定冠詞が省略されるのはどういうときか。

### さらなるヒント

所有対象が家族の場合、定冠詞を省略する。ただし、例外がある。

### 答え

「アイルランド人の姉」のように修飾がつく場合は定冠詞がつく。

# /details
